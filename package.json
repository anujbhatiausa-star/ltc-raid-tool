{
  "name": "ltc-raid-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "npx kill-port 3001 5173 || true",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  },
  "engines": {
    "node": ">=18.11.0"
  }
}
